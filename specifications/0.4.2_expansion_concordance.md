# Feature 0.4.2 – Expansion concordance between SF and blood

## Cíl

Rozšířit verzi `0.4.1`.

Kromě detekce klonů chceme určit:

```text
Pokud je klon expandovaný v jednom kompartmentu,
s jakou pravděpodobností je expandovaný i ve druhém kompartmentu?

Hlavní otázky:

P(SF expanded | blood expanded)
P(blood expanded | SF expanded)
Verze

Zvýšit verzi projektu na:

0.4.2

Aktualizovat:

pyproject.toml
tcr_bcr_tools/__init__.py
CHANGELOG.md
Vstup

Použij:

paired_detection_table.csv

Minimální sloupce:

patient
cell_type
clonotype_key
sf_cells
blood_cells
sf_fraction
blood_fraction
detected_in_sf
detected_in_blood
shared_clone
Definice expanze

Přidej podporu pro více strategií definice expandovaného klonu.

1. Fraction threshold

Klon je expandovaný, pokud:

fraction >= threshold

Například:

sf_fraction >= 0.005
blood_fraction >= 0.005

Výchozí strategie:

expansion-method = fraction
expansion-threshold = 0.005
2. Cell count threshold

Klon je expandovaný, pokud:

n_cells >= threshold

Například:

sf_cells >= 5
blood_cells >= 5
3. Quantile threshold

Klon je expandovaný, pokud patří mezi největší klony v daném:

patient + cell_type + compartment

Například top 5 %:

quantile = 0.95
Nové sloupce

Vytvoř rozšířenou tabulku:

expansion_status_table.csv

Sloupce:

patient
cell_type
clonotype_key
sf_cells
blood_cells
sf_fraction
blood_fraction
sf_expanded
blood_expanded
expansion_status
detected_in_sf
detected_in_blood
shared_clone
sf_expanded
True pokud je klon expandovaný v SF
blood_expanded
True pokud je klon expandovaný v blood
expansion_status

Jedna z hodnot:

expanded_both
expanded_sf_only
expanded_blood_only
not_expanded
Souhrnná tabulka

Vytvoř:

expansion_concordance_summary.csv

Sloupce:

cell_type
expansion_method
expansion_threshold
n_clones
n_sf_expanded
n_blood_expanded
n_expanded_both
n_expanded_sf_only
n_expanded_blood_only
p_sf_expanded_given_blood_expanded
p_blood_expanded_given_sf_expanded
jaccard_expanded
Definice
p_sf_expanded_given_blood_expanded =
n_expanded_both / n_blood_expanded
p_blood_expanded_given_sf_expanded =
n_expanded_both / n_sf_expanded
jaccard_expanded =
n_expanded_both / (n_sf_expanded + n_blood_expanded - n_expanded_both)

Pokud je jmenovatel 0, nastav hodnotu na NaN.

Souhrn po pacientech

Vytvoř také:

expansion_concordance_by_patient.csv

Sloupce:

patient
cell_type
expansion_method
expansion_threshold
n_clones
n_sf_expanded
n_blood_expanded
n_expanded_both
p_sf_expanded_given_blood_expanded
p_blood_expanded_given_sf_expanded
jaccard_expanded
Vizualizace

Vytvoř složku:

outputs/expansion/
1. Bar plot statusů

Soubor:

outputs/expansion/expansion_status_counts.png

Zobraz počty:

expanded_both
expanded_sf_only
expanded_blood_only
not_expanded
2. Scatter plot frakcí

Soubor:

outputs/expansion/sf_vs_blood_fraction_scatter.png

X osa:

blood_fraction

Y osa:

sf_fraction

Každý bod = klon.

Použij log škálu pro obě osy.

Nuly řeš pomocí malé pseudo-hodnoty:

pseudocount = 1e-6
3. Patient-level heatmap-like matrix

Soubor:

outputs/expansion/patient_concordance_matrix.png

Matice:

řádky = patient
sloupce = cell_type
hodnota = p_sf_expanded_given_blood_expanded

Použij matplotlib, ne seaborn.

CLI rozšíření

Přidej nový příkaz:

python -m tcr_bcr_tools.expansion_concordance \
  --input ./outputs/paired_detection_table.csv \
  --output-dir ./outputs \
  --expansion-method fraction \
  --expansion-threshold 0.005

Podporované parametry:

--expansion-method fraction|cells|quantile
--expansion-threshold 0.005
--cell-type CD4
--no-plots

Pro quantile interpretuj --expansion-threshold jako kvantil, např.:

0.95
Nový modul

Přidej:

tcr_bcr_tools/expansion_concordance.py

Doporučené funkce:

def classify_expansion(
    df: pd.DataFrame,
    method: str = "fraction",
    threshold: float = 0.005,
) -> pd.DataFrame:
    ...

def summarize_expansion_concordance(df: pd.DataFrame) -> pd.DataFrame:
    ...

def summarize_expansion_concordance_by_patient(df: pd.DataFrame) -> pd.DataFrame:
    ...

def plot_expansion_status_counts(df: pd.DataFrame, output_path: Path) -> None:
    ...

def plot_sf_vs_blood_fraction_scatter(df: pd.DataFrame, output_path: Path) -> None:
    ...

def plot_patient_concordance_matrix(summary_df: pd.DataFrame, output_path: Path) -> None:
    ...

def run_expansion_concordance_analysis(
    input_path: Path,
    output_dir: Path,
    method: str = "fraction",
    threshold: float = 0.005,
    cell_type: str | None = None,
    make_plots: bool = True,
) -> None:
    ...
Implementační poznámky
Načti paired_detection_table.csv.
Volitelně filtruj podle cell_type.
Klasifikuj expanzi v SF a blood.
Vytvoř expansion_status.
Ulož expansion_status_table.csv.
Vytvoř globální souhrn.
Vytvoř souhrn po pacientech.
Pokud není --no-plots, vytvoř grafy do:
outputs/expansion/
Testy

Přidej:

tests/test_expansion_concordance.py

Otestuj minimálně:

fraction definici expanze,
cells definici expanze,
quantile definici expanze,
status expanded_both,
status expanded_sf_only,
status expanded_blood_only,
status not_expanded,
výpočet P(SF expanded | blood expanded),
výpočet P(blood expanded | SF expanded),
výpočet jaccard_expanded,
chování při nulovém jmenovateli,
souhrn po pacientech,
filtr --cell-type,
režim --no-plots,
vytvoření všech tří PNG grafů.
README

Doplň sekci:

Version 0.4.2 – Expansion concordance

Popiš:

rozdíl mezi detekcí a expanzí,
tři způsoby definice expanze,
význam sf_expanded, blood_expanded,
význam expansion_status,
hlavní pravděpodobnosti:
P(SF expanded | blood expanded)
P(blood expanded | SF expanded)
příklad spuštění CLI.
CHANGELOG

Doplň:

## [0.4.2] - Expansion concordance between compartments

### Added
- Classification of expanded clones in SF and blood.
- Support for fraction, cell-count and quantile-based expansion thresholds.
- Expansion status table.
- Global expansion concordance summary.
- Patient-level expansion concordance summary.
- Expansion status bar plot.
- SF vs blood fraction scatter plot.
- Patient-level concordance matrix.
Kritéria dokončení

Feature je hotová, pokud:

vzniká expansion_status_table.csv,
vzniká expansion_concordance_summary.csv,
vzniká expansion_concordance_by_patient.csv,
lze spočítat P(SF expanded | blood expanded),
lze spočítat P(blood expanded | SF expanded),
fungují metody fraction, cells, quantile,
funguje filtr --cell-type,
funguje režim --no-plots,
vznikají grafy v outputs/expansion/,
všechny testy projdou přes pytest,
projekt má verzi 0.4.2.